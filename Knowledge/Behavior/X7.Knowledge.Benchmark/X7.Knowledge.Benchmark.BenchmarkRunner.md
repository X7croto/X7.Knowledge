# public sealed class BenchmarkRunner

Namespace `X7.Knowledge.Benchmark`. Declarado em `X7.Knowledge.Benchmark/BenchmarkRunner.cs`.

## Métodos

- `public static CanonicalJson ToJson(QuestionSet set, IReadOnlyList<Measurement> measurements, string solutionDigest, int projectCount)`
- `public static IReadOnlyList<Measurement> Measure(QuestionSet set, string solutionRoot, string knowledgeRoot, IReadOnlyCollection<string> baseCapabilities = null)`
- `public static double? Median(IReadOnlyList<Measurement> measurements)`

