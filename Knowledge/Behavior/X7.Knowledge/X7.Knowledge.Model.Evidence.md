# public sealed record Evidence

Namespace `X7.Knowledge.Model`. Declarado em `X7.Knowledge/Model/Evidence.cs`.

## Propriedades

- `public required IReadOnlyList<KnowledgeId> Observations { get; init; }`
- `public required KnowledgeId Id { get; init; }`
- `public required string Capability { get; init; }`
- `public required string Kind { get; init; }`
- `public required string Producer { get; init; }`

## Métodos

- `public static Evidence Create(string kind, IEnumerable<KnowledgeId> observations, string producer, string capability)`

