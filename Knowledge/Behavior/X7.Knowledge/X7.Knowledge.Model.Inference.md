# public sealed record Inference

Namespace `X7.Knowledge.Model`. Declarado em `X7.Knowledge/Model/Inference.cs`.

## Propriedades

- `public Frequency Frequency { get; init; }`
- `public required Confidence Confidence { get; init; }`
- `public required InferenceProvenance Provenance { get; init; }`
- `public required KnowledgeId Evidence { get; init; }`
- `public required KnowledgeId Id { get; init; }`
- `public required KnowledgeId Subject { get; init; }`
- `public required ObservationPayload Payload { get; init; }`
- `public required string Kind { get; init; }`

## Métodos

- `public static Inference Create(string kind, KnowledgeId subject, ObservationPayload payload, Evidence evidence, Confidence confidence, InferenceProvenance provenance, Frequency frequency = null)`

