# public sealed class KnowledgeModelBuilder

Namespace `X7.Knowledge.Model`. Declarado em `X7.Knowledge/Model/KnowledgeModelBuilder.cs`.

## Propriedades

- `public IReadOnlyCollection<Evidence> Evidence { get; }`
- `public IReadOnlyCollection<Inference> Inferences { get; }`
- `public IReadOnlyCollection<Observation> Observations { get; }`

## Métodos

- `public Evidence AddEvidence(Evidence evidence)`
- `public Inference AddInference(Inference inference)`
- `public KnowledgeModel Build(string modelVersion, string compilerVersion, AcquisitionLevel level, IReadOnlyList<string> capabilities, string inputDigest, string msBuildVersion = null)`
- `public Observation Add(Observation observation)`
- `public Observation Add(string kind, KnowledgeId subject, ObservationPayload payload, Provenance provenance)`

