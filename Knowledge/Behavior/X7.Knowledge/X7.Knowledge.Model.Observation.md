# public sealed record Observation

Namespace `X7.Knowledge.Model`. Declarado em `X7.Knowledge/Model/Observation.cs`.

## Propriedades

- `public required KnowledgeId Id { get; init; }`
- `public required KnowledgeId Subject { get; init; }`
- `public required ObservationPayload Payload { get; init; }`
- `public required Provenance Provenance { get; init; }`
- `public required string Kind { get; init; }`

## Métodos

- `public static Observation Create(string kind, KnowledgeId subject, ObservationPayload payload, Provenance provenance)`

