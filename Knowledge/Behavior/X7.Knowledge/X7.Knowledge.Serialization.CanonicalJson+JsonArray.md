# public sealed record JsonArray

Namespace `X7.Knowledge.Serialization`. Declarado em `X7.Knowledge/Serialization/CanonicalJson.cs`.

## Construtores

- `public JsonArray(IReadOnlyList<CanonicalJson> Items)`

## Propriedades

- `public IReadOnlyList<CanonicalJson> Items { get; init; }`

