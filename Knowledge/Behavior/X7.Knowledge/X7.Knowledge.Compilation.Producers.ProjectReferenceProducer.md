# public sealed class ProjectReferenceProducer

Namespace `X7.Knowledge.Compilation.Producers`. Declarado em `X7.Knowledge/Compilation/Producers/ProjectReferenceProducer.cs`.

## Propriedades

- `public string Capability { get; }`
- `public string Name { get; }`

## Métodos

- `public ValueTask ProduceAsync(CompilationContext context, CancellationToken cancellationToken)`

