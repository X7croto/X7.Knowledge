# public sealed class TypeRelationProducer

Namespace `X7.Knowledge.Compilation.Producers`. Declarado em `X7.Knowledge/Compilation/Producers/TypeRelationProducer.cs`.

## Construtores

- `public TypeRelationProducer(IReadOnlyList<SourceCompilation> sources)`

## Propriedades

- `public string Capability { get; }`
- `public string Name { get; }`

## Métodos

- `public ValueTask ProduceAsync(CompilationContext context, CancellationToken cancellationToken)`

