# public sealed class ProjectProducer

Namespace `X7.Knowledge.Compilation.Producers`. Declarado em `X7.Knowledge/Compilation/Producers/ProjectProducer.cs`.

## Propriedades

- `public string Capability { get; }`
- `public string Name { get; }`

## Métodos

- `public ValueTask ProduceAsync(CompilationContext context, CancellationToken cancellationToken)`

